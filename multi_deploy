pipeline { 
    agent any
    stages {
        stage('Prepare') { 
            steps { 
                script {
                	//SVN_APPLCATION_NAME
                	['Cat', 'Dog', 'Elephant'].eachWithIndex { animalName, index ->
    					println "${index+1}. Animal ${animalName}"
					}
                }
            }
        }
        stage('Build'){
            steps {
                script {
                	
                }
            }
        }

    }
}