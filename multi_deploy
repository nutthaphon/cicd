pipeline { 
    agent any
    stages {
        stage('Prepare') { 
            steps { 
                script {
                	
                	SVN_APPLICATION_NAME.eachWithIndex { animalName, index ->
    					println "${index+1}. Animal ${animalName}"
					}
                }
            }
        }
        stage('Build'){
            steps {
                script {
                	print SVN_APPLICATION_NAME
                }
            }
        }

    }
}